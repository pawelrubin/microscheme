module HaScheme.Primitives where

import HaScheme.Ast

-- primitives :: [(String, [SchemeVal] -> ThrowsError SchemeVal)]
-- primitives =
--   [ -- numeric
--     ("+", numericBinOp (+)),
--     ("-", numericBinOp (-)),
--     ("*", numericBinOp (*)),
--     ("/", numericBinOp div),
--     ("^", numericBinOp (^))
--   ]